// Referencing to the id="hero-button"
const button = document.getElementById("hero-button");

// Action when the button was clicked
button.addEventListener("click", () => {
    alert("Go buy yourself a food!");
});